# 天工智梯 · 智能工厂申报全流程 AI 智能体系统

面向拟申报 **智能工厂梯度培育**（基础级 → 先进级 → 卓越级 → 领航级）的制造企业，提供从
**自测画像、政策问答、大纲生成、申报书研判** 到 **专家评定反馈** 的一站式 AI 辅助。

> 定位：大连市智能制造产业协会「AI 专家顾问」——线上引流 + 线下专家交付。输出为**指导性意见**，非权威结论。

---

## 一、快速开始

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 配置模型接口（OpenAI 兼容，支持智谱/DeepSeek/通义等国产模型）
cp .env.example .env
#   编辑 .env 填入 OPENAI_API_KEY / OPENAI_BASE_URL / MODEL_NAME

# 3. 启动（单命令，浏览器自动打开 http://localhost:8501）
bash run.sh
#   或： streamlit run app.py
```

**离线能力**：未配置模型接口时，「② 政策标准问答」与「⑤ 专家评定反馈」可完全离线运行
（知识库 + 意图识别 / 关键词 Mock 引擎），模块⑤ 三大模块（初筛/增强/评分）均开箱即用；
①③ ④ 会在调用时给出配置提示，配置后即通过 OpenAI 兼容接口生成完整报告。

---

## 二、统一系统架构

```
天工智梯/
├── app.py                     # 统一入口：落地页 + 侧边导航 + 全局配置状态
├── core/                      # 共享内核
│   ├── llm.py                 # 统一 OpenAI 兼容客户端（call_llm / 配置检测 / 免责声明）
│   ├── knowledge.py           # 统一政策知识库（移植 B）：等级阈值/政策条款/案例/奖补/40场景
│   ├── a_prompts.py           # 模块①提示词（企业画像 + 改造路径，原样移植 A 板块）
│   ├── c_prompts.py           # 模块③提示词（COSTAR + 问卷 schema + 生成规则 + F1-F8，原样移植 C 板块）
│   └── e_prompts.py           # 模块⑤提示词与知识库（忠实转录专家端整合包 01/02/03 + 40场景）
├── knowledge/
│   └── expert_review_prompt.md  # 模块④六步预审提示词（原样移植 D 板块）
├── expert_pack/               # 模块⑤ 专家端整合包真实素材（V3.3，用户直接提供，供透明核查）
│   ├── 01_系统提示词_SystemPrompt.md
│   ├── 02_能力契约_Capabilities.md
│   ├── 03_知识库_KnowledgeBase.md
│   ├── 05_参考实现_ReferenceImplementation.md
│   ├── README_整合说明.md
│   ├── 智能体卡片_agent.card.json
│   └── 1784556664*_sample{1,2,3}_*.txt  # 3 个样例申报书（先进级通过/不合格拦截/基础级通过）
├── pages/
│   ├── 1_企业画像与等级推荐.py     # ① 六维度评分 + 冲稳保概率 + 改造路径与成本估算
│   ├── 2_政策标准问答.py           # ② 离线意图识别 + 知识库问答（移植 B 引擎）
│   ├── 3_申报书大纲生成.py         # ③ 10模块问卷 + 离线缺口分析 + 分级大纲 + F1-F8
│   ├── 4_申报书研判优化.py         # ④ 六步预审 + 🔴🟡🔵 三级标记
│   └── 5_专家评定反馈.py           # ⑤ 专家端三模块流水线：初筛→增强标注→评分建议（对齐整合包）
├── requirements.txt
├── .env.example
└── run.sh
```

**数据流（串联五大模块）**：① 画像推荐 → 输出当前/目标等级 → ② 政策问答随时答疑 →
③ 依等级生成大纲/缺口 → ④ 对草稿做六步研判 → ⑤ 专家基于研判做最终评定与反馈，
形成「企业自检 → AI 生成 → 专家把关」的闭环。

---

## 三、五大模块与原始板块对应关系

| 模块 | 功能 | 原始板块 | 实现方式 |
|------|------|---------|---------|
| ① 企业画像与等级推荐 | 六维度评分、四级达标度、冲稳保概率、改造路径与成本 | A（企业画像与智能等级推荐） | 原样移植 A 的提示词与表单逻辑 |
| ② 政策标准问答 | 等级对标/指标/奖补/流程/政策/案例 | B（政策标准互动问答） | 移植 B 的 knowledge-base 为 Python，重写意图识别+生成引擎 |
| ③ 申报书大纲生成 | 10模块问卷、缺口分析、分级大纲、F1-F8 | C（大纲生成） | 原样移植 C 的 COSTAR 提示词与问卷 schema |
| ④ 申报书研判优化 | 六步预审、合规/一致性/证据评估、三级标记 | D（申报书研判） | 原样移植 D 的六步预审提示词，注入政策标准库+案例库 |
| ⑤ 专家评定反馈 | 初筛(PASS/REJECT/PENDING)→增强标注→6维度100分评分+ P0/P1/P2 建议 | E（专家端整合包 V3.3） | **忠实对齐整合包**：转录 01/02/03 提示词与知识库，离线关键词引擎复现 05 参考实现 |

---

## 四、数据与政策依据

- 政策框架：《智能工厂梯度培育管理办法》（工信部联通装〔2025〕262号）、《要素条件（2025年版）》、《智能制造典型场景参考指引（2025年版）》（8 大环节 40 场景）、GB/T 39116-2020 成熟度模型。
- 知识库来源：IMA 共享知识库「智能工厂政策」(ID: 7482270705267738) 及《惠企政策30条》等公开政策，移植自各板块原始素材。
- 奖补标准：数字化车间 ≤50 万；先进级 ≤100 万；卓越级及以上 ≤200 万；叠加省/市配套与贴息（示意值，以当年政策为准）。

---

## 五、重要说明（来源与边界）

1. **专家端（模块⑤）已用整合包真实内容对齐**：用户已直接提供「天工智梯_智能体整合包」（V3.3）全部文件，
   模块⑤ 据此忠实落地三大模块流水线——① 初步筛查（形式审查+基础要求+目标等级一票否决，输出 `PASS/REJECT/PENDING_EXPERT` 结构化判定）
   → ② 内容增强标注（原文-标准-关键词三方对照 + 40 场景覆盖矩阵 + 红/黄/蓝问题分级）
   → ③ 评分辅助与建议（6 维度 100 分制 + 结论映射 + P0/P1/P2 动作级修改清单）。
   提示词（01/02/03）已逐字转录至 `core/e_prompts.py`，知识库与 40 场景、6 维度评分表、结论映射一并内置；
   离线关键词引擎（`pages/5_*.py` 内 `analyze_doc/screening/enhancement_skeleton/build_suggestions`）复现整合包 `05_参考实现`
   的 Mock 逻辑（含"替代人工操作"不判手工生产等负向词排除），**无需 API Key 即可开箱运行**；配置接口后三大模块可调用真实大模型生成完整报告。
   整合包真实素材置于 `expert_pack/` 目录供透明核查。
2. **政策阈值与案例** 来自各板块已成功下载的本地素材；网盘侧对部分文件（含 D 的 policy-standards.md / historical-cases.md）当前同样返回密钥失效，
   模块④所需的政策标准库与案例库已由 `core/knowledge.py` 依据可用素材重建并在运行时注入，确保「依据可溯」。
3. **逐级申报**：四级梯度原则上不可跳级，跳级面临一票否决；系统在各模块均强制提示。
4. **免责声明**：所有输出均标注「AI 生成、仅供参考，最终以专家评定及工信部门审核为准」。

---

## 六、运行环境要求

- Python 3.10+
- 可访问的 OpenAI 兼容大模型接口（模块②③④⑤需联网调用；模块②可离线）
- 浏览器（Streamlit 自动打开）
